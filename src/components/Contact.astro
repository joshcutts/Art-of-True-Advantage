<section id="contact" class="contact-section">
  <div class="contact-container">
    <h2 class="contact-header">Contact</h2>
    
    <div class="contact-grid">
      <a href="mailto:media@artoftrueadvantage.com" class="contact-card">
        <span class="contact-label">MEDIA</span>
        <span class="contact-email">media@artof<em>trueadvantage</em>.com</span>
      </a>

      <a href="mailto:comments@artoftrueadvantage.com" class="contact-card">
        <span class="contact-label">COMMENTS</span>
        <span class="contact-email">comments@artof<em>trueadvantage</em>.com</span>
      </a>

      <a href="mailto:business@artoftrueadvantage.com" class="contact-card">
        <span class="contact-label">BUSINESS</span>
        <span class="contact-email">business@artof<em>trueadvantage</em>.com</span>
      </a>
    </div>
  </div>
</section>

<style>
  .contact-section {
    width: 100%;
    padding: 40px 20px;
    background-color: #000000;
    display: flex;
    justify-content: center;
    scroll-margin-top: 80px;
  }

  .contact-container {
    max-width: 1200px;
    width: 100%;
    text-align: center;
  }

  .contact-header {
    font-size: 36px;
    color: white;
    margin-bottom: 50px;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .contact-grid {
    display: flex;
    justify-content: center;
    gap: 30px;
    flex-wrap: wrap; /* Allows cards to stack on mobile */
  }

  .contact-card {
    background: rgba(0, 0, 0, 0.05);
    border: 1px solid rgba(215, 137, 65, 0.3); /* Subtle orange border */
    padding: 30px;
    min-width: 280px;
    text-decoration: none;
    border-radius: 4px;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .contact-card:hover {
    background: rgba(215, 137, 65, 0.1);
    transform: translateY(-5px);
    border-color: #d78941;
  }

  .contact-label {
    color: #d78941; /* The signature orange */
    font-weight: 900;
    font-size: 1.2rem;
    letter-spacing: 1px;
  }

  .contact-email {
    color: white;
    font-weight: bold;
    font-size: 1rem;
  }

  .contact-email em {
    font-style: italic;
  }

  @media (max-width: 768px) {
    .contact-section {
      padding: 40px 20px; /* Explicitly forces equal left/right padding on the screen edge */
    }

    .contact-grid {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 20px 0;        /* 20px gap between stacked cards, 0px gap on the sides */
      width: 100%;
    }
    
    .contact-card {
      min-width: 0;       
      width: 100%;        
      max-width: 320px;   /* Limits width so they look crisp and centered */
      box-sizing: border-box; /* Crucial: includes the 30px card padding inside the width */
    }
  }
</style>