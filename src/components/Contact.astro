<section id="contact" class="contact-section">
  <div class="contact-container">
    
    <div class="contact-header-striking">
      <h2 class="contact-title">Contact</h2>
    </div>
    
    <div class="contact-grid">
      <a href="mailto:media@artoftrueadvantage.com" class="contact-card">
        <span class="contact-label">MEDIA</span>
        <span class="contact-email">media@artof<em>trueadvantage</em>.com</span>
      </a>

      <a href="mailto:comments@artoftrueadvantage.com" class="contact-card">
        <span class="contact-label">COMMENTS</span>
        <span class="contact-email">comments@artof<em>trueadvantage</em>.com</span>
      </a>

      <a href="mailto:business@artoftrueadvantage.com" class="contact-card">
        <span class="contact-label">BUSINESS</span>
        <span class="contact-email">business@artof<em>trueadvantage</em>.com</span>
      </a>
    </div>
  </div>
</section>

<style>
/* 1. Universal box-sizing reset for absolute boundary safety */
.contact-section,
.contact-container,
.contact-grid,
.contact-card,
.contact-label,
.contact-email {
  box-sizing: border-box !important;
}

.contact-section {
  width: 100%;
  padding: 60px 20px; /* Matched to Author section vertical padding */
  background-color: #000000;
  display: flex;
  justify-content: center;
  scroll-margin-top: 80px;
}

.contact-container {
  max-width: 1000px; /* Matched to Author container width */
  width: 100%;
  text-align: center;
}

/* ==========================================
   UNIFIED CONTACT STRIKING HEADER STYLES
   ========================================== */
.contact-header-striking {
  display: flex !important;
  flex-direction: row !important;
  align-items: center !important; 
  justify-content: center !important;
  width: 100% !important;
  margin: 0 0 45px 0 !important; /* Slightly more room down to the grid items */
}

.contact-header-striking h2.contact-title {
  font-family: "Inter", "Helvetica Neue", sans-serif !important;
  font-size: 2rem !important; /* Matched identically to Author title */
  font-weight: 800 !important;
  text-transform: uppercase !important;
  letter-spacing: 0.2em !important; 
  color: #ffffff !important; 
  margin: 0 !important;
  padding: 0 !important;
  line-height: 1 !important;
  white-space: nowrap !important; 
  border: none !important;
  background: transparent !important;
  text-decoration: none !important;
}

/* Pseudo-elements handle the horizontal tracks seamlessly */
.contact-header-striking::before,
.contact-header-striking::after {
  content: "" !important;
  display: block !important;
  flex: 1 !important; 
  height: 1px !important; 
  min-height: 1px !important;
  background-color: #d78941 !important; /* Muted amber accent line */
  opacity: 1 !important;
  visibility: visible !important;
}

.contact-header-striking::before {
  margin-right: 30px !important;
}

.contact-header-striking::after {
  margin-left: 30px !important;
}

/* ==========================================
   GRID & CARD STYLES
   ========================================== */
.contact-grid {
  display: flex;
  justify-content: center;
  gap: 30px;
  flex-wrap: wrap; 
  width: 100%;
}

.contact-card {
  background: rgba(255, 255, 255, 0.02); /* Cleaned dark transparency */
  border: 1px solid rgba(255, 255, 255, 0.15); /* Sleek, structural border matching photo subtle lines */
  padding: 30px;
  min-width: 280px;
  flex: 1; /* Allows them to distribute horizontally beautifully */
  max-width: 310px;
  text-decoration: none;
  border-radius: 4px;
  transition: all 0.2s ease;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.contact-card:hover {
  background: rgba(215, 137, 65, 0.08);
  transform: translateY(-4px);
  border-color: #d78941;
}

.contact-label {
  color: #d78941; 
  font-family: "Inter", "Helvetica Neue", sans-serif;
  font-weight: 800;
  font-size: 0.9rem; /* Cleaned up tag footprint */
  letter-spacing: 0.15em;
}

.contact-email {
  color: #ffffff;
  font-weight: 500;
  font-size: 0.95rem;
  transition: color 0.2s ease;
}

.contact-card:hover .contact-email {
  color: #ffffff;
}

.contact-email em {
  font-style: italic;
}

/* ==========================================
   MOBILE RESPONSIVE OVERRIDES
   ========================================== */
@media (max-width: 768px) {
  .contact-section {
    padding: 40px 20px !important; 
  }

  .contact-container {
    width: 100% !important;
    max-width: 100% !important;
    overflow: hidden !important; 
  }

  .contact-header-striking {
    margin-bottom: 30px !important;
    padding: 0 5px !important;
    display: flex !important;
    flex-direction: row !important;
    flex-wrap: nowrap !important;
    width: 100% !important;
  }

  .contact-header-striking h2.contact-title {
    font-size: 1.5rem !important; /* Unified mobile heading scale precisely */
    letter-spacing: 0.15em !important;
    white-space: nowrap !important; 
    text-align: center !important;
    flex: 0 1 auto !important;
  }

  /* BULLETPROOF FLEX FIXES FOR THE PSEUDO TRACKS */
  .contact-header-striking::before,
  .contact-header-striking::after {
    display: inline-block !important;
    background-color: #d78941 !important;
    min-width: 20px !important;
    flex: 1 0 30px !important; 
  }

  .contact-header-striking::before { margin-right: 15px !important; }
  .contact-header-striking::after { margin-left: 15px !important; }

  .contact-grid {
    flex-direction: column !important;
    align-items: center !important;
    gap: 20px !important;        
    width: 100% !important;
  }
  
  .contact-card {
    min-width: 0 !important;       
    width: 100% !important;        
    max-width: 340px !important;   
    padding: 20px 15px !important; 
    text-align: center !important;
    flex: none !important;
  }

  .contact-email {
    font-size: 0.9rem !important; 
    word-break: break-all;         
  }
}
</style>